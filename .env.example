# MongoDB Connection String
MONGODB_URI=mongodb://127.0.0.1:27017/hrms-ai-next

# JSON Web Token Secret
# Used for signing and verifying authentication tokens.
# You can generate a secure secret using: openssl rand -hex 32
JWT_SECRET=

# Encryption Key for sensitive data (e.g., API Keys)
# This MUST be a 64-character hex string (32 bytes).
# You can generate a secure key using: openssl rand -hex 32
ENCRYPTION_KEY=

# Google Gemini API Key
# Required for all AI-powered features.
GEMINI_API_KEY=
